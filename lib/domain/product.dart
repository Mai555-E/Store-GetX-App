class Product {
  double id, price;
  String title, image, size, description;

  Product({required this.id, required this.size, required this.price, required this.title, required this.image, required this.description});
}
